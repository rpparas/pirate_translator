import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
    input: {
        marginTop: "1.5rem",
    },
    button: {
        fontSize: "1rem",
        fontWeight: "bold",
        marginTop: "1rem",
        height: 50,
    },
    spinner: {
        marginLeft: 5,
    },
}));
